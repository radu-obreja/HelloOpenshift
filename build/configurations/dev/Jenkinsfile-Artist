pipeline {
  agent {
	label 'maven'
  }
  stages {
    stage('Config Image') {
      steps {
        script {
          openshift.withCluster() {
			openshift.selector('dc', 'hello-openshift-artist').env("--from=configmap/hello-openshift-config")
          }
        }
      }
    }	
  }
}